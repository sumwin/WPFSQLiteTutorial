<UserControl x:Class="WPFUI.Controls.ClientControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFUI.Controls"
             mc:Ignorable="d" 
             FontFamily="Open Sans"
             FontSize="18"
             d:DesignHeight="425" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Row 0 -->
        <StackPanel Orientation="Horizontal"
                    Grid.Row="0"
                    Margin="5 5 5 10">
            <StackPanel Orientation="Vertical">
                <TextBlock>Client</TextBlock>
                <ComboBox x:Name="ClientDropDown"
                          Width="150"/>
            </StackPanel>
            <Button x:Name="EditButton">Edit</Button>
            <Button x:Name="NewButton">New</Button>
        </StackPanel>
        
    </Grid>
</UserControl>
